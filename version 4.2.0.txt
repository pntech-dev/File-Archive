- Added the ability to add instructions in PDF format
- Added a button to open the instruction file directly from the application

Bugs:
    - Fixed a bug that could cause the table to be updated incorrectly.
    - Fixed a bug that caused the instruction file extensions to be displayed incorrectly.
    - Fixed a bug that could cause the interface to remain disabled when uploading a file.
    - Fixed a bug that caused notifications to be displayed incorrectly after deleting a group.